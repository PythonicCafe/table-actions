import { TableActions } from "./table-actions.js";
import { toNormalForm, newElement } from "./utils.js";

export default TableActions;

/*
 * Exporting all utils functions in case we
 * need to use outside
 */
export { toNormalForm, newElement };
